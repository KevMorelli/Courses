@model List<WebNorthWnd.Region>

<h2>Index</h2>

<table>

    @foreach (var region in Model)
    {
        <tr>
            <td>@region.RegionID</td>
            <td>@region.RegionDescription</td>
        </tr>
    }

</table>